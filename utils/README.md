# Utils

Thư mục `utils` chứa các hàm hỗ trợ cho pipeline suy luận:

- nạp danh sách ảnh hợp lệ từ thư mục đầu vào
- lưu báo cáo CSV
- gán trạng thái phát hiện cho từng ảnh

Logic chính hiện nằm trong `helperFunctions.py`.
