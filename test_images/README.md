# Test Images

Thư mục này chứa dữ liệu đầu vào mẫu để kiểm thử hệ thống.

- Ảnh công nhân có và không có mũ bảo hộ.
- Một video mẫu để kiểm tra pipeline suy luận cho video.

Các tệp trong thư mục này có thể dùng trực tiếp với `main.py` hoặc web demo `app.py`.
